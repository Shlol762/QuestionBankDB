# The base URL of the FastAPI backend API.
# This is used by the Vite frontend to make API calls.
VITE_API_BASE_URL=http://localhost:8000

# Optional: comma-separated host allowlist for Vite dev server.
# Example: VITE_ALLOWED_HOSTS=my-tunnel.ngrok-free.app,localhost
# VITE_ALLOWED_HOSTS=
